[package]
name = "virtual-core"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "virtual core: machines, profiles, QEMU/libretro command lines, QMP protocol — no Tauri"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
